!!!
%html
  %head
    %title SketchStitchStart
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    %link{:rel => "stylesheet", :href => "http://cdnjs.cloudflare.com/ajax/libs/normalize/3.0.1/normalize.min.css"}
    %link{:rel => "stylesheet", :href => "http://maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css"}
    = csrf_meta_tags
  %body
    %header
      .wrapper.clearfix
        #logo= link_to "SketchStitchStart", root_path
        %nav 
        - if designer_signed_in?
          Welcome #{link_to current_designer.name, edit_designer_registration_path } #{link_to "Sign out", destroy_designer_session_path, :method => :delete}
          = link_to "Submit New Design", new_design_path, class: "button"
        - else
          = link_to "Sign in", new_designer_session_path
          or #{link_to "Sign up", new_designer_registration_path, class: "button"}

    %p.notice= notice
    %p.alert= alert
    

    = yield